<li class="nav-header">
  By Provider Credentials
</li>
<% for row in @search.facet(:provider_credentials).rows.first(5) %>
  <li>
    <% if params[:provider_credentials].blank? %>
    <h6>
      <%= link_to row.value, medicare_payments_path(:search => params[:search], :provider_state => params[:provider_state], :provider_gender => params[:provider_gender], :provider_type => params[:provider_type], :provider_credentials => row.value) %>
      <span class="pull-right"><%= number_with_precision(row.count, :precision => 0, :delimiter => ",") %></span>
    </h6>
    <% else %>
      <h6>
      <strong><%= row.value %></strong> (<%= link_to "remove", medicare_payments_path(:search => params[:search], :provider_state => params[:provider_state], :provider_gender => params[:provider_gender], :provider_type => params[:provider_type], :provider_credentials => nil) %>)
      </h6>
    <% end %>
  </li>
<% end %>